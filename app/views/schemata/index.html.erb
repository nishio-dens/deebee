<script id="sidebarTop" type="text/x-jsrender">
    <div class="col-xs-12">
    <div class="form">
    <div class="form-group">
      <label>Project</label>
      <%= select_tag 'projects', options_for_select(@projects.map { |v| [v.name, v.id] }), class: 'form-control projects' %>
    </div>
    <div class="form-group">
      <label>Version</label>
      <%= select_tag 'versions', options_for_select(@versions.map { |v| [v.name, v.id] }), class: 'form-control versions' %>
    </div>
    </div>
    </div>
</script>


<script id="mainToolbar" type="text/x-jsrender">
  <div class="box" style="margin-bottom: 0px">
    <div class="box-body">
      <div class="col-xs-7">
        <div class="btn-group">
          <%= link_to '#', class: "btn btn-default" do %>
            Add Division
          <% end %>
          <%= link_to '#', class: "btn btn-default" do %>
            Table Settings
          <% end %>
          <%= link_to '#', class: "btn btn-default" do %>
            Project Settings
          <% end %>
        </div>
      </div>

      <div class="col-xs-5">
        <p>
          Tableの説明がここに入ります
        </p>
      </div>
    </div><!-- /.box-body -->
  </div>
</script>
